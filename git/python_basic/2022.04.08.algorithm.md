#### 백준 알고리즘 
- 조건문



```python
#2884번문제
# 알람시계

# 입력 :첫째 줄에 두 정수 H와 M이 주어진다. (0 ≤ H ≤ 23, 0 ≤ M ≤ 59) 그리고 
#이것은 현재 상근이가 설정한 놓은 알람 시간 H시 M분을 의미한다.

#입력 시간은 24시간 표현을 사용한다. 24시간 표현에서 하루의 시작은 0:0(자정)이고, 
#끝은 23:59(다음날 자정 1분 전)이다. 시간을 나타낼 때, 불필요한 0은 사용하지 않는다.

# 출력:첫째 줄에 상근이가 창영이의 방법을 사용할 때, 설정해야 하는 알람 시간을 출력한다. (입력과 같은 형태로 출력하면 된다.)
# 이렇게 해야 런타임 에러가 안난다



H = int(input())
M = int(input())


if M-45 < 0 :
    if H - 1 < 0 :
        H = 23
        print(H,M + 15)
    else :
        print(H - 1, M + 15) #H가 0보다 크면서 45분 이하 시에 M에 +15를 해준다
else :
     print(H ,M - 45) #M45분보다 크면 45분을 뺴준다


#분이 45분보다 클  경우는 양수 이지만 음수일 경우를 생각해야 된다
#시가 0시일 경우는 분이 45분 이하 일 시 23시가 되야한다

```

    10
    40
    9 55
    
