```python
#### 백준 알고리즘 -나머지
# 두 자연수 A와 B가 있을 때, A%B는 A를 B로 나눈 나머지 이다. 예를 들어, 7, 14, 27, 38을 3으로
# 나눈 나머지는 1, 2, 0, 2이다. 

# 수 10개를 입력받은 뒤, 이를 42로 나눈 나머지를 구한다. 
# 그 다음 서로 다른 값이 몇 개 있는지 출력하는 프로그램을 작성하시오.

i = int(input())

for  i in range(10): #i를 받는다
    i%42=n i%42을 #나머지값을 받고 
    print(conunt(n!=)) #서로다른 나머지 값을 출력한다
    
#     서로 다른 나머지 값을 출력하는 방법을 모르겠다



```


```python
arr = []
for i in range(10):
    n = int(input())
    arr.append(n % 42) #리스트에 값넣기
arr = set(arr)  #카운트가 아닌 리스트 형태로 만들어서 set으로 중복을 제거하면된다..
print(len(arr)) #len함수로 개수 출력


#리스트 형태로 변환해서 하기
#문제는 이해 갔는데 아직 무슨무슨 함수를 언제 어떻게 적용해야할지 부족한거 같다
```
